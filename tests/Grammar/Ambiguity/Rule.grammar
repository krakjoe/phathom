lexer: "Ambiguity.lexer";

item:
    (ALPHA EQUALS ALPHA) {
        return ["low" => (string) $1];
    }
    |
    (ALPHA EQUALS ALPHA) {
        return ["high" => (string) $1];
    }
;

unit: item+;
