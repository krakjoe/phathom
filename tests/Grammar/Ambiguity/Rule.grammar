lexer: "Ambiguity.lexer";

item:
    (ALPHA EQUALS ALPHA) {
        return $this->returnThings("low", (string) $1);
    }
    |
    (ALPHA EQUALS ALPHA) {
        return $this->returnThings("high", (string) $1);
    }
;

unit: item+;
