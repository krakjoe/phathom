lexer: "Basic.lexer";

context: "\pharos\phathom\tests\Grammar\File";

item:
    (ALPHA EQUALS NUM) {
        return $this->returnThings((string) $1, (string) $3);
    };

# Last rule is entry point if no unit specified
_: item+;